import { Mail, Lock, Loader2, FileText, Home, FileQuestion, ArrowLeft } from 'lucide-react';

export const Icons = {
    Mail,
    Lock,
    Loader: Loader2,
    FileText,
    Home,
    FileQuestion,
    ArrowLeft
};

